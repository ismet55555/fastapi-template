# -*- mode: yaml -*-
# vim: set filetype=yaml
# atom: set grammar=yaml
---
version: "3.8"

services:
  app:
    build: .
    env_file:
      - .env
    ports:
      - "8000:8000"

    # privileged: true
    # devices:
    #   - /dev/ttyS0:/dev/ttyS0
